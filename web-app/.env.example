# Copy to .env.local, which is gitignored, and fill in your bot.
# The bot's username without a leading @, and the same bot whose token the ledger holds.
VITE_TELEGRAM_BOT_USERNAME=example_finance_bot
