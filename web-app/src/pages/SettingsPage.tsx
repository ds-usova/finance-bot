// TODO: reads the preferences on mount, renders the CurrencyPicker on the stored currency, and saves the picked
// code back through `replacePreferences` only when a person asks to, rather than as it is picked.
export function SettingsPage() {
  return null;
}
