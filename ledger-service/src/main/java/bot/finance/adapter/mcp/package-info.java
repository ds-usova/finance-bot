/**
 * The MCP server's tools and their wire types. Every Spring AI MCP annotation and result type stays inside this
 * package, never crossing into {@code domain}/{@code application}.
 */
package bot.finance.adapter.mcp;
