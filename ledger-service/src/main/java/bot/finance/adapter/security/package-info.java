/**
 * Everything about who may call the service over HTTP: the filter chain, the token decoder, the token minter
 * and the JWKS endpoint. The only package that constructs {@code AuthenticatedUserId}.
 */
package bot.finance.adapter.security;
