### Changing an entry's category — PATCH /api/v1/expenses/{id}
# Refiles one of the signed-in person's entries under a different category, named by its id alone. The document
# replaces /categoryId and nothing else, and the answer is the entry as it now stands. Needs an open session and
# the CSRF header, since it writes.

@host = http://localhost:1000
# Set from the fb_session cookie a sign-in response sets — see ../session/sign-in.http.
@sessionCookie = paste-the-fb_session-cookie-value-here
# Set from the XSRF-TOKEN cookie a "read the session" response returns — see ../session/current-session.http.
@csrfToken = paste-the-XSRF-TOKEN-cookie-value-here

### An entry of the signed-in person's own, named by its id.
PATCH {{host}}/api/v1/expenses/12
Content-Type: application/json-patch+json
Cookie: fb_session={{sessionCookie}}; XSRF-TOKEN={{csrfToken}}
X-XSRF-TOKEN: {{csrfToken}}

[
  {
    "op": "replace",
    "path": "/categoryId",
    "value": 42
  }
]
