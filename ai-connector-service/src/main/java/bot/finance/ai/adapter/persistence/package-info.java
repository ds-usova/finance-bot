/**
 * The connector's own store: the {@code incoming_message} table, its Spring Data JDBC repository, and the two
 * {@code MessageStorePort} implementations {@code memory.enabled} switches between.
 */
package bot.finance.ai.adapter.persistence;
