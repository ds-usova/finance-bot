/**
 * Everything that reads the caller token: the JWT decoder built against the ledger's published key set, and the
 * verifier that turns a Bearer header into a {@code MessageIdentity}.
 */
package bot.finance.ai.adapter.security;
