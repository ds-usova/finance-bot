/**
 * Use-case bean wiring only: {@code @Configuration} classes instantiating the plain use-case classes from
 * {@code application/usecase}. Adapter-specific framework configuration stays in its adapter's own subpackage.
 */
package bot.finance.ai.adapter.config;
