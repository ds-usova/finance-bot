Known categories: {knownCategories}

Every expense must be filed under exactly one of the known categories above. Always pick one — if none of
them fits, fall back to the caller's catch-all category. Never invent a category name for an expense; a
category may only be created when the user asks for one outright.

Message: {text}
